# BySergiMM/homebrew-tap

Homebrew tap for [OmniMac](https://github.com/BySergiMM/OmniMac), everything your Mac is missing in one menu-bar app.

```bash
brew install --cask BySergiMM/tap/omnimac
```

Updates arrive through the app itself (Sparkle) and through `brew upgrade --cask omnimac`.
